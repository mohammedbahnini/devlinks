import { useContext } from 'react'
import { useStore } from 'zustand'
// import { FakeStoreContext } from './editor-page-provider'
import { Button } from '@/components/ui/button';


export default function FakeConsumerComponent() {
    // let init = null;
    // const store = useContext(FakeStoreContext);
    // if (store) {
    //     init = useStore(store, s => s.init);
    //     console.log(init);
    //     return (
    //         <Button>click me</Button>
    //     )
    // }


    return (
        null
    )
}